query SearchNotes($query: String!) {
  notesCollection(
    filter: {
      deletedAt: { is: NULL }
      or: [
        { title: { ilike: $query } }
        { body: { ilike: $query } }
      ]
    }
    orderBy: [{ createdAt: DescNullsLast }]
  ) {
    edges {
      node {
        id
        title
        body
        createdAt
        updatedAt
        deletedAt
      }
    }
  }
}